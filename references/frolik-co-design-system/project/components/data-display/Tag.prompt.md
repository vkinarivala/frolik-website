One-line: Pill chip for portfolio category filters and metadata tags; ink outline at rest, fills with `tone` when `selected`.

```jsx
<Tag selected tone="coral">Residential</Tag>
<Tag tone="mint">Commercial</Tag>
```
