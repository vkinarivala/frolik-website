One-line: Text input with the brand's 2px ink outline and coral focus ring; pass `label` and optional `hint`.

```jsx
<Input label="Your name" placeholder="Jane Doe" />
<Input label="Email" hint="We reply within 2 days" type="email" />
```
